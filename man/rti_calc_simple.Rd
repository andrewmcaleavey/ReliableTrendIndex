% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy.R
\name{rti_calc_simple}
\alias{rti_calc_simple}
\title{Legacy helper: rti_calc_simple}
\usage{
rti_calc_simple(values, sem, time = NULL, level = 0.95, na.rm = FALSE)
}
\arguments{
\item{values}{Numeric vector of within-person observations (length >= 2).}

\item{sem}{Positive numeric scalar: standard error of measurement.}

\item{time}{Optional numeric vector of time indices (same length as \code{values});
if omitted, uses \code{1:length(values)}.}

\item{level}{Confidence level for slope intervals (default 0.95).}

\item{na.rm}{Logical; drop non-finite \code{(values, time)} pairs? Default \code{FALSE}.}
}
\value{
A list with \code{rmaObj} (the reliableTrend fit), \code{error_var} (sem^2),
and \code{fit} (alias to the reliableTrend), for legacy compatibility.
}
\description{
Minimal wrapper used in old examples and vignettes. Computes an RTI fit
from a vector of scores and a single SEM (standard error of measurement).
Returns a list containing fields older examples expect:
\itemize{
\item rmaObj: the model object (here we store the reliableTrend fit)
\item error_var: sigma^2 used (i.e., sem^2)
}
}
\examples{
rti_calc_simple(c(47.5, 32.5), sem = 3.35)
}
