% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rti_by.R
\name{rti_by}
\alias{rti_by}
\title{RTI for grouped (panel) data}
\usage{
rti_by(data, id, time, y, sd, r, na.rm = FALSE, level = 0.95)
}
\arguments{
\item{data}{A data frame in long format.}

\item{id}{Grouping variable (column) identifying units (e.g., person id).}

\item{time}{Time/index variable (column). Can be equally or irregularly spaced.}

\item{y}{Outcome variable (column).}

\item{sd}{Either a single positive numeric (applied to all groups) or the
name of a column in \code{data} providing per-row \code{sd}. If a column is
supplied, it must be constant within each \code{id}.}

\item{r}{Either a single numeric in \link{0,1} (applied to all groups) or the
name of a column in \code{data} providing per-row \code{r}. If a column is
supplied, it must be constant within each \code{id}.}

\item{na.rm}{Logical. Drop non-finite \code{(y, time)} pairs within groups?}

\item{level}{Confidence level for slope intervals.}
}
\value{
A data frame with one row per \code{id}, containing summary fields
and a list column \code{fit} holding each group's \code{reliableTrend} object.
}
\description{
Compute the Reliable Trend Index within groups (e.g., per person) from a
long-format data frame.
}
\examples{
set.seed(1)
df <- data.frame(
  id = rep(letters[1:3], each = 6),
  time = rep(1:6, times = 3),
  y = c(5 + 0.8*(1:6) + rnorm(6, 0, 3),
        10 - 0.5*(1:6) + rnorm(6, 0, 3),
        2 + 0.0*(1:6) + rnorm(6, 0, 3))
)
out <- rti_by(df, id, time, y, sd = 8, r = 0.85)
out
}
