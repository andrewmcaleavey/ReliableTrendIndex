% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rti_by_person.R
\name{rti_by_person}
\alias{rti_by_person}
\title{RTI for a single person (deprecated)}
\usage{
rti_by_person(
  data,
  id,
  y,
  time = NULL,
  r,
  sd_single = NULL,
  sd_method = c("global", "per_person", "baseline"),
  baseline_time = NULL,
  p = 0.05,
  verbose = FALSE,
  na_rm = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} with columns referenced by \code{id}, \code{y}, and (optionally) \code{time}.}

\item{id}{Bare column name identifying the person.}

\item{y}{Bare column name for the outcome/score.}

\item{time}{Optional bare column name for time; if missing, the within-person
order \verb{1..n} is used.}

\item{r}{Numeric scalar in \eqn{[0, 1]}: reliability used to derive measurement error.}

\item{sd_single}{Optional positive numeric scalar SD used for all persons. If \code{NULL},
\code{sd_method} determines the SD source.}

\item{sd_method}{Character scalar, one of \code{"global"}, \code{"per_person"}, or \code{"baseline"};
ignored if \code{sd_single} is provided.}

\item{baseline_time}{If \code{sd_method = "baseline"}, the time value treated as baseline
per person. If \code{NULL}, uses the earliest time per person.}

\item{p}{Numeric scalar in \eqn{(0, 1)}; two-sided alpha (default \code{0.05}).}

\item{verbose}{Logical; if \code{TRUE}, return detailed columns (legacy format).}

\item{na_rm}{Logical; if \code{TRUE} (default) drops incomplete rows.}
}
\value{
If \code{verbose = FALSE} (default), a \code{data.frame} with columns
\code{id}, \code{n}, \code{slope_hat}, \code{RTI}, \code{crit}, \code{RTI_cat}. If \code{verbose = TRUE},
returns additional diagnostics as in prior releases.
}
\description{
\code{rti_by_person()} is \strong{deprecated}; use \code{\link[=rti_by]{rti_by()}} instead (e.g., filter to a
single person or run \code{rti_by()} on the whole data and slice that person's row).
This remains for backward compatibility and mirrors the legacy logic.
}
\details{
Per person, fits \code{y ~ time} via OLS and computes an RTI z-statistic using
\code{SE = (sd * sqrt(1 - r)) / sqrt(S_xx)}, where \code{sd} comes from \code{sd_single} or
\code{sd_method}, and \code{S_xx = sum((t - mean(t))^2)}.
}
\seealso{
\code{\link[=rti_by]{rti_by()}}
}
\keyword{internal}
