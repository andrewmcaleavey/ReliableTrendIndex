% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy.R
\name{forest_to_reg_plot}
\alias{forest_to_reg_plot}
\title{Turning metafor-style forest results into a regression-style plot}
\usage{
forest_to_reg_plot(x, StError = NULL, level = 0.95, ...)
}
\arguments{
\item{x}{A \code{reliableTrend} or a \code{metafor::rma*} object.}

\item{StError}{Optional standard error input used by some legacy calls; ignored
when \code{x} is a \code{reliableTrend}.}

\item{level}{Confidence level for intervals (default 0.95).}

\item{...}{Ignored.}
}
\value{
A \code{ggplot} object.
}
\description{
Works with either a \strong{metafor} \code{rma*} object (if metafor is installed)
or a \code{reliableTrend} object (from \code{rti()} / \code{reliableTrend()}).
}
\examples{
test <- rti_calc_simple(c(47.5, 32.5), 3.35)
forest_to_reg_plot(test$rmaObj, StError = sqrt(test$error_var))
}
